package com.hashmap.haf.functions.services

object ConfigurationKeys {

	val PREFIX = "ignite"
	val TOTAL_COUNT = s"$PREFIX.totalCount"
	val MAX_PER_NODE_COUNT = s"$PREFIX.maxPerNodeCount"
	val CACHE_NAME = s"$PREFIX.cacheName"
}
